version: 2

project_name: crivo

builds:
  - id: crivo
    main: ./cmd/crivo/
    binary: crivo
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}
    flags:
      - -trimpath

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

brews:
  - name: crivo
    repository:
      owner: guilherme11gr
      name: homebrew-tap
    description: "Lightweight quality gate for code analysis — open source SonarQube alternative"
    homepage: "https://github.com/guilherme11gr/crivo"
    license: "MIT"
    install: |
      bin.install "crivo"
    test: |
      system "#{bin}/crivo", "version"

nfpms:
  - id: default
    package_name: crivo
    description: "Lightweight quality gate for code analysis"
    maintainer: "Guilherme Revoredo"
    license: MIT
    formats:
      - deb
      - rpm
